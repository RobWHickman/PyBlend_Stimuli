The scripts to generate trees using Blender

Random Forests are scripts to be run in python through blender and the sapling add-on to generate single trees:
  - 1.3.0 is the latest large rewrite of this
  - 1.3.3 is the latest small rewrite (added textures and generalised filepaths)
  
Grid Arrange Trees is a script in R to take images from the imagebin folder and plot them in a grid along two parameter axis
